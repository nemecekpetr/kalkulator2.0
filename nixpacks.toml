# Cache bust: 2026-08-17-v3 (tini as PID 1 to reap zombie Chromium processes)
[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "tini"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
PUPPETEER_EXECUTABLE_PATH = "/nix/var/nix/profiles/default/bin/chromium"
